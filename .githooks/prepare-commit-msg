#!/bin/sh
# Strip Cursor attribution trailers when a message file is present.
set -eu

. "$(dirname "$0")/cursor-attribution.sh"

rewrite_cursor_identity

msg_file="${1:-}"
[ -n "$msg_file" ] && [ -f "$msg_file" ] || exit 0

strip_cursor_trailers "$msg_file"
