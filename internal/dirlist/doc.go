// Package dirlist renders a read-only directory listing for the picker
// preview: eza with fixed arguments when it is installed, otherwise a plain
// builtin listing. Nothing here is user-configurable.
package dirlist
